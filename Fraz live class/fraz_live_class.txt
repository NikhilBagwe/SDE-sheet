== BINARY SEARCH ==========================================

* To find mid element, don't use -> (s+e)/2 instead use
	s + (e-s)/2 -> To avoid integer overflow

* Geometric progression : n/2^k = 1
  on solving;
  n = 2^k
  logn = log2^k
  logn = klog2
  k = logn/log2 = logn
